<!DOCTYPE html>
<html>

<head>
    <title>Page not found!</title>
    {{>headCss}}
</head>

<body>
    <div class="wrapper">
        {{>navbar}}
        {{>header}}
        <div class="container">
            <div class="row">
                {{error}}
            </div>
        </div>
    </div>
    {{>footer}}
</body>

</html>